<script setup>
import { computed, useAttrs } from 'vue';
import { cn } from '@/lib/utils';

const attrs = useAttrs();
const classes = computed(() => cn('flex flex-col space-y-1.5 p-6', attrs.class));
</script>

<template>
  <div v-bind="{ ...attrs, class: undefined }" :class="classes">
    <slot />
  </div>
</template>
